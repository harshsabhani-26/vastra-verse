{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "export DIRECT_URL=${DIRECT_URL:-$DATABASE_URL} && npx prisma generate && npx prisma migrate deploy && npm start",
        "healthcheckPath": "/api/health",
        "healthcheckTimeout": 120,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10,
        "numReplicas": 1
    },
    "preDeployCommand": "export DIRECT_URL=${DIRECT_URL:-$DATABASE_URL} && node scripts/validate-env.js && npx prisma migrate deploy"
}