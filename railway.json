{
  "_comment": "RAILWAY CONFIG — DISABLED (Switched to Vercel). Re-enable below if needed.",
  "_disabled_schema": "https://railway.app/railway.schema.json",
  "_disabled_build": {
    "builder": "NIXPACKS"
  },
  "_disabled_deploy": {
    "preDeployCommand": "export DIRECT_URL=${DIRECT_URL:-$DATABASE_URL} && node scripts/validate-env.js && npx prisma migrate deploy",
    "startCommand": "export DIRECT_URL=${DIRECT_URL:-$DATABASE_URL} && npx prisma generate && npm start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1
  }
}